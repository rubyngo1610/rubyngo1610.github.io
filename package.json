{
  "name": "rubyngo-portfolio",
  "private": true,
  "scripts": {
    "predev":    "node scripts/copy-resume.mjs",
    "dev":       "npm run dev --prefix mvp-keys",
    "prebuild":  "node scripts/copy-resume.mjs",
    "build":     "npm run build --prefix mvp-keys",
    "preview":   "npm run preview --prefix mvp-keys"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
